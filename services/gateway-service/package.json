{
  "name": "@bison/gateway-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@bison/contracts": "workspace:*",
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.2.0",
    "undici": "^7.2.0"
  },
  "devDependencies": {
    "@bison/tsconfig": "workspace:*",
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
